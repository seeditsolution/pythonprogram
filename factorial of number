# python program to find factorial

n=int(input("Enter Number"))

fact = 1

for i in range(1,n+1):
  
   fact=fact*i
   
# in python indentation is required in order to enter inside the loop

# indentaion can be done by giving two tab generally
  
print(fact)  
  
