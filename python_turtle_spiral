import turtle

x = 10

arrow = turtle.Turtle()

arrow.speed(10)

def pentagon(arg):
	for x in range(5):
		arrow.forward(arg)
		arrow.left(72)
	arrow.left(x)
	x += 0.75

for x in range(1, 200, 2):
	pentagon(x)

turtle.done()
