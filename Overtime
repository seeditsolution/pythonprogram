try:
    hrs = float(input("Enter Hours:"))
    rate = float(input("Enter the Rate:"))
except:
    print ("Error, please enter numeric input ")
    quit()
print(hrs,rate)
if hrs > 40:
    print( (40* rate) + ((hrs - 40 ) * 1.5 * rate))	#overtime
else:
    print( hrs * rate)          # regular
	
